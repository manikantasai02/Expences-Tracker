services:
  - type: web
    name: expenses-tracker
    env: python
    plan: free
    buildCommand: "echo 'No build step required -- stdlib only'"
    startCommand: "python backend/server.py"
    envVars:
      - key: RENDER
        value: "true"
